"""Pure workflow logic: typed errors, immutable values and the guard registry.

Nothing here imports Django or another app, so the transition rules are testable on
`SimpleTestCase` with no database at all.
"""
