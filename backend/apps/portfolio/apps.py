"""Django app configuration for the portfolio context."""

from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    """Clients, team members, projects and the command-center read model."""

    name = "apps.portfolio"
